# Example Task Prompt

Open a pull request against `main` that adds a small helper function and tests.

Requirements:

- create the branch `task/example-001`
- make the PR title include `[task:example-001]`
- keep the changes small and easy to review
- make sure both `lint` and `test` pass
